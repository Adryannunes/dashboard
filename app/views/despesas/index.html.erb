<table>
  <thead>
    <tr>
     <th>Date</th>
     <th>Description</th>
     <th>Type</th>
     <th>quantia</th>
    </tr>
  </thead>
  <tbody>
  <% @despesas.each do |e| %>
   <tr>
    <td><%= e.date %></td>
    <td><%= e.description %></td>
    <td><%= e.type %></td>
    <td><%= e.quantia %></td>
   </tr>
  <% end %>
  </tbody>
</table>