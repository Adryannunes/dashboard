<%= line_chart Despesa.group_by_day(:date).count %>

<%= column_chart Despesa.group_by_day_of_week(:date, format: "%a").count %>

<%= bar_chart Despesa.group(:date).sum(:date) %>

<%= area_chart Despesa.group_by_minute(:date).maximum(:date) %>
<!--
 <table>
  <thead>
    <tr>
     <th>Date</th>
     <th>Description</th>
     <th>Type</th>
     <th>quantia</th>
    </tr>
  </thead>
  <tbody>
  <% @despesas.each do |e| %>
   <tr>
    <td><%= e.date %></td>
    <td><%= e.description %></td>
    <td><%= e.type %></td>
    <td><%= e.quantia %></td>
   </tr>
  <% end %>
  </tbody>
</table>
-->
